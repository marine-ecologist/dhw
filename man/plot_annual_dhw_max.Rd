% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_max_dhw.R
\name{plot_annual_dhw_max}
\alias{plot_annual_dhw_max}
\alias{plot_max_dhw}
\title{Plot annual maximum DHW time series}
\usage{
plot_max_dhw(dhw_rast)
}
\arguments{
\item{dhw_rast}{A \code{terra::SpatRaster} of Degree Heating Weeks with a valid time vector.}

\item{pad_years_left}{Integer, years to extend the left x-axis beyond min year (default 1).}

\item{pad_years_right}{Integer, years to extend the right x-axis beyond max year (default 1).}

\item{x_break_by}{Integer, spacing between x-axis breaks in years (default 2).}

\item{fill_limits}{Numeric length-2, limits for the fill gradient (default \code{c(0, 26)}).}

\item{fill_midpoint}{Numeric, midpoint for \code{ggplot2::scale_fill_gradient2} (default 3).}
}
\value{
A list with:
\itemize{
\item \code{data}: \code{tibble} with columns \code{year} (integer) and \code{maxdhw} (double)
\item \code{plot}: \code{ggplot} object of the annual maxima bar chart
}
}
\description{
Compute annual maxima from a DHW \code{SpatRaster} and return a wide data.frame
with columns \code{year} and \code{maxdhw}, plus a ggplot showing bars for each year.
}
\examples{
\dontrun{
out <- plot_annual_dhw_max(ningaloo_climatology$dhw)
out$data
print(out$plot)
}
}
